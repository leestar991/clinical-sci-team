# 深度研究报告生成任务

> **⚡ 这是一份完整的自执行任务指令。请立即开始执行步骤0，不得以任何理由停下来询问用户方向或确认。所有判断自行做出，全程不得追问。**

## 任务背景与输入材料

你是一名资深药物研发专家，刚参加 2026 年 AACR 年会，现在需要基于以下已上传材料，独立完成一份深度研究报告：

- **主题文件** `{TASK_NAME}.md`：核心关注主题，包含海报内容、关键论点与机制介绍，是报告聚焦方向的来源
- **参考文献**（已全部上传）：与主题高度相关的大会海报，每份 PDF 均已自动转换为同名 `.md` 文件——这些 `.md` 文件是原始海报的文本提取结果，是你的**阅读材料**，不是待生成的总结产物

## 最终交付物

**输出路径（唯一，不可更改）：**
`/mnt/user-data/outputs/{TASK_NAME}_report.md`

**验收标准：** 该文件必须实际存在于磁盘上，且内容包含完整七章正文与附录，字数不少于 8000 字。

---

## ⚡ 核心执行原则（最高优先级，凌驾一切）

### 原则 A：禁止追问（零例外）

**这是自执行任务，用户不在线，不会回答任何问题。** 任何情况下不得向用户提问、请求确认、询问偏好。遇到歧义直接选最合理方案执行，继续推进。

**特别注意：** 本任务是一份**完整的结构化执行计划**，已明确规定了所有步骤和决策规则。当你认为"需要确认方向"时，答案已经在本文档的步骤定义里——按步骤顺序执行即是正确方向。**不存在需要用户回答的问题。**

**⚠️ 绝对禁止调用 `ask_clarification` 工具。** 调用该工具将立即中断任务，无法恢复。无论何种原因，都不得调用此工具。

**以下场景的标准处理方式（无需确认，直接执行）：**
- 认为需要确认执行方向 → 按步骤0→1→2→3→4→5顺序继续执行下一步
- 认为输入材料是"总结"而非"明确指令" → 本文档本身就是完整指令，立即从步骤0开始执行
- 有两种合理方式 → 选第一种（继续顺序执行）
- 是否先整合再继续 → 不整合，继续顺序执行
- 是否继续下一批 → 是，立即继续
- 步骤1内部有多个批次 → 按文件顺序逐批处理完所有文件
- 无法操作文件系统 → 使用 write_file / str_replace_editor 等可用工具写入，不跳过
- 步骤4内容太长 → 分多次连续输出完整正文，不压缩，不用"预览"代替，直到全部章节输出完毕
- 收到包含"A/B/C/D/E选哪个"的摘要 → 忽略选项，继续从当前未完成步骤执行
- 收到上下文摘要，不知道当前进度 → 不追问，直接执行文件验证（原则B），根据验证结果决定从哪步继续
- 收到上下文摘要，担心重复工作 → 不追问，按原则C的续接规则执行（已完成步骤跳过，从下一步继续）

### 原则 B：任务完成的唯一判定标准
**只有当以下条件同时满足时，任务才算完成：**
1. `/mnt/user-data/outputs/{TASK_NAME}_report.md` 文件实际存在
2. 已用 `present_files` 工具展示该文件
3. 已输出包含文件路径和字数统计的完成摘要

**以下情况均不算完成，必须继续执行：**
- 仅输出了"报告结构预览"或"内容示例" → 必须输出完整正文
- 仅在对话中显示了报告内容但未写入文件 → 必须调用工具写入文件
- 步骤4输出了"（模拟）" "（此处将生成...）" 等占位文字 → 必须替换为实际内容
- 步骤5输出了"（由于无法操作文件系统，模拟此过程）" → 必须调用实际工具写入

**如果收到的上下文摘要中写着"Task Status: Completed"或"步骤5已完成"等字样，必须立即执行以下验证：**
```bash
ls -lh "/mnt/user-data/outputs/{TASK_NAME}_report.md" 2>/dev/null && echo "FILE_EXISTS" || echo "FILE_MISSING"
```
- 若输出 `FILE_EXISTS`：调用 `present_files` 展示文件，输出完成摘要，任务结束。
- 若输出 `FILE_MISSING`：**无论摘要怎么说，立即从步骤4开始重新执行，直到文件生成为止。**

### 原则 C：上下文续接规则
收到任何形式的"对话摘要"、"上下文快照"、"工作流状态说明"时：
1. **不得停顿、不得追问**
2. 立即执行原则 B 的文件验证
3. 根据验证结果和摘要中的进度状态决定从哪步继续：
   - 最终文件存在 → 呈现并结束
   - 最终文件不存在 + 摘要显示步骤1进行中 → 立即继续步骤1的下一批文件
   - 最终文件不存在 + 步骤1已全部完成 → 从步骤2继续
   - 最终文件不存在 + 步骤2已完成 → 从步骤3继续
   - 最终文件不存在 + 步骤3已完成 → 从步骤4继续
   - 无法判断进度 → 从步骤1开始

**续接后的第一句话必须是执行动作，禁止以下任何开头：**
- ❌ "我已接收这份上下文..."
- ❌ "你希望我下一步..."
- ❌ "当前有两种方案..."
- ❌ "需要确认..."
- ❌ "你提供的是阶段性总结，但没有给出新的明确交付目标..."
- ❌ "为了避免误判方向，我需要先确认..."
- ❌ "你希望我下一步具体做哪一项？A/B/C/D/E..."
- ❌ "I need the correct project status before proceeding..."
- ❌ "🤔 I need the correct project status..."
- ❌ "为避免重复工作或跳过未完成分析，我需要确认当前进度..."
- ❌ 任何包含"need to confirm"、"need to verify"、"need clarification"、"请确认"、"能否告知"的句子
- ✅ 直接输出正在执行的动作（如"开始处理Batch 3: LB197、D3S-003、RCZY-698"或"执行文件验证..."）

**⚠️ 禁止调用 `ask_clarification` 工具（零例外）：**
- 无论何时，无论何种原因，绝对禁止调用 `ask_clarification` 工具
- 调用该工具会中断整个任务，导致任务无法完成
- 遇到任何不确定情况 → 按步骤顺序继续执行，不询问
- 收到上下文摘要显示"进度不明" → 从步骤1开始重新执行，不询问

### 原则 D：错误自恢复
遇到任何工具报错、文件读取失败、路径不存在，**不停顿，不报告，直接**：
1. 记录错误到当前输出文件末尾（一行）
2. 跳过该子操作，继续执行下一个子操作
3. 绝不因单个错误中断整体流程

### 原则 E：反幻觉（写作全程强制）

**严禁编造以下任何数据或信息：**
- IC50、EC50、KD、TGI、ORR、PFS、OS、DOR、CR率
- 样本量、剂量、给药频率、治疗周期
- PK参数（AUC、Cmax、t½、F%、Cl、Vd）
- 毒理参数（NOAEL、MTD、DLT发生率）
- 专利号、NCT编号、ChiCTR编号
- 公司名称、合作关系、融资金额、临床阶段

**未披露信息的处理规则：**
- 文档中未出现的信息 → 写"文档未披露"，附录标注 G-xx
- 检索未找到的信息 → 写"本次检索未发现公开记录"，附录标注 G-xx
- 不同来源数据不一致 → 正文简述差异，附录标注 X-xx
- 推断内容 → 明确标注"推断"，不写成已验证事实

**证据编号体系（全报告统一使用）：**
- `D-xx`：文档事实（来自上传的海报 .md 文件中明确展示的信息）
- `DB-xx`：数据库事实（来自 UniProt、ChEMBL、ClinicalTrials.gov 等）
- `L-xx`：文献事实（来自 PubMed、bioRxiv、期刊论文）
- `CT-xx`：临床登记事实（来自 ClinicalTrials.gov、ChiCTR、CDE）
- `P-xx`：专利事实（来自 Google Patents、CNIPA、USPTO）
- `C-xx`：公司公告事实（来自官网、新闻稿、年报、投资者材料）
- `X-xx`：不一致或需人工复核的信息
- `G-xx`：文档未披露或检索未发现的信息缺口

正文中直接使用编号引用（如 [D-01]、[CT-03]），所有原始内容统一放附录。

### 原则 F：强制产出验证（每步必须执行，零例外）

**核心规则：每个步骤完成后，必须立即验证该步骤的预期产出文件是否存在于磁盘上。验证失败则重试当前步骤，最多3次。**

**验证方法统一为：**
```
ls /mnt/user-data/workspace/[预期路径]
```

**每步预期产出与验证规则：**

| 步骤 | 预期产出文件 | 验证命令 | 失败处理 |
|------|------------|---------|---------|
| 步骤0 | `workspace/{TASK_NAME}_plan.md` | `ls -lh workspace/{TASK_NAME}_plan.md` | 立即重写 |
| 步骤1（每批） | `workspace/doc_summaries/[文件名]_解读.md` ×3 | `ls workspace/doc_summaries/ \| wc -l` | 对缺失文件重新 write_file |
| 步骤1（全部） | doc_summaries 下文件数 = 参考文献总数 | 逐一核对 | 补写缺失文件 |
| 步骤2 | `workspace/master_summary.md` | `ls -lh workspace/master_summary.md` | 立即重写 |
| 步骤3（每次） | `workspace/search_results/[主题].md` | `ls workspace/search_results/` | 立即重写当次 |
| 步骤3（全部） | search_results 下文件数 = 6 | `ls workspace/search_results/ \| wc -l` | 补写缺失主题 |
| 步骤4（每章） | `workspace/report_chapters/ch[N].md` | `ls workspace/report_chapters/` | 立即重写当章 |
| 步骤4（全部） | report_chapters 下文件数 ≥ 8（ch0~ch7） | 逐一核对 | 补写缺失章节 |
| 步骤5 | `outputs/{TASK_NAME}_report.md` | `ls -lh outputs/{TASK_NAME}_report.md` | 立即重写 |

**验证失败时的处理流程：**
```
verify_output(expected_file):
    result = ls(expected_file)
    if FILE_EXISTS:
        return PASS
    else:
        # 重试最多3次
        for attempt in [1, 2, 3]:
            re-execute write_file for the missing content
            result = ls(expected_file)
            if FILE_EXISTS:
                return PASS
        # 3次都失败：记录错误，继续下一个子任务（原则D）
        log_error("验证失败3次: " + expected_file)
        continue_next_subtask()
```

**⚠️ 验证是写入后的强制动作，不是可选步骤：**
- 每次 `write_file` 之后必须立即 `ls` 确认
- 禁止"假设写入成功"而跳过验证
- 禁止在对话中声明"已验证"但未实际执行 `ls`

---

## 执行步骤（按序全部完成，不得跳步，不得合并）

---

### 【步骤 0】分析材料并创建任务计划文件

**触发时机：** 收到第一条消息（含文件附件）后立即执行，不得跳过。

**执行流程（两阶段，均必须完成）：**

#### 阶段一：初步分析（先读后写）

在写入计划文件之前，必须先完成以下分析，将结果用于计划文件中：

1. 扫描 `/mnt/user-data/uploads/` 目录，列出所有文件（`.md`、`.pdf`、`.jpg` 等），统计数量
2. 读取主题文件 `{TASK_NAME}.md`，提取：主题方向（1-2句）、核心化合物/靶点列表、关键研究问题、**Modality 类型**（小分子/ADC/分子胶/双抗等）
3. 浏览参考文献列表，按文件名推断每份文献的研究方向（无需逐一读取内容）
4. 基于上述分析，规划步骤3检索主题（6个，与具体任务相关，替换默认模板中的占位内容）

#### 阶段二：写入计划文件

将分析结果写入：`/mnt/user-data/workspace/{TASK_NAME}_plan.md`

**计划文件初始模板（严格按此格式写入，所有 [] 替换为实际内容）：**

```markdown
# 任务计划：{TASK_NAME}

## 基本信息
- 任务名称：{TASK_NAME}
- 最终报告路径：/mnt/user-data/outputs/{TASK_NAME}_report.md
- 计划创建时间：[当前时间]
- 最后更新时间：[当前时间]

## 上传文件清单（[N] 个文件）

### 主题文件
- {TASK_NAME}.md — 核心关注主题

### 参考文献（[M] 个，均为 PDF→Markdown 转换结果）
[逐行列出 uploads/ 中除 {TASK_NAME}.md 以外的所有 .md 文件名]

## 执行进度

| 步骤 | 名称 | 状态 | 备注 |
|------|------|------|------|
| 步骤0 | 创建计划文件 | ✅ 完成 | [计划文件创建时间] |
| 步骤1 | 解读全部文档 | ⏳ 进行中 | 共 [M] 份文件，已完成 0/[M] |
| 步骤2 | 生成综合总结 | ⬜ 待开始 | — |
| 步骤3 | 在线检索补充 | ⬜ 待开始 | — |
| 步骤4 | 撰写最终报告 | ⬜ 待开始 | — |
| 步骤5 | 保存文件呈现 | ⬜ 待开始 | — |

## 步骤1 文件处理进度

| 序号 | 文件名 | 状态 | 解读文件路径 |
|------|--------|------|-------------|
[逐行列出每个参考文献文件，初始状态为 ⬜ 待处理]

## 步骤3 检索计划

| 优先级 | 检索主题 | 状态 | 结果文件 |
|--------|---------|------|---------|
| 1 | [主题1，根据任务填写] | ⬜ 待检索 | — |
| 2 | [主题2] | ⬜ 待检索 | — |
| 3 | [主题3] | ⬜ 待检索 | — |
| 4 | [主题4] | ⬜ 待检索 | — |
| 5 | [主题5] | ⬜ 待检索 | — |
| 6 | [主题6] | ⬜ 待检索 | — |

> ⚠️ 上表检索主题**必须根据步骤0阶段一的分析结果替换为与本任务实际相关的6个主题**，不得使用默认占位内容。

## 主题分析摘要（步骤0阶段一输出）

- **主题方向：** [从{TASK_NAME}.md提取，1-2句]
- **Modality 类型：** [小分子 / ADC / 分子胶 / 双抗 / 其他]
- **核心化合物/靶点：** [列出]
- **关键研究问题：** [列出2-3个]
- **参考文献概览：** [按文件名推断的研究方向，一行一条]

## 最终输出

- 报告文件：/mnt/user-data/outputs/{TASK_NAME}_report.md
- 目标字数：不少于 8000 字
- 完成状态：⬜ 未完成
```

**⚠️ 步骤0产出验证（写入后立即执行）：**
```
ls -lh /mnt/user-data/workspace/{TASK_NAME}_plan.md
```
验证通过才能继续。验证失败立即重写。

**步骤0完成，必须立即输出此行，然后立即开始步骤1：**
> ✅ 步骤0完成：计划文件已创建 → /mnt/user-data/workspace/{TASK_NAME}_plan.md → 立即执行步骤1

---

### 【计划文件更新规则】（贯穿全程，每步完成后必须执行）

**在以下时机必须更新计划文件（追加写入或覆盖更新对应行）：**

1. **步骤1每完成一批文件**：更新"步骤1 文件处理进度"表格中对应行的状态为 `✅ 已完成`，更新步骤进度备注（如"已完成 3/12"），更新"最后更新时间"
2. **步骤1全部完成**：更新执行进度表步骤1状态为 `✅ 完成`，步骤2状态改为 `⏳ 进行中`
3. **步骤2完成**：更新执行进度表步骤2为 `✅ 完成`，步骤3改为 `⏳ 进行中`，记录信息缺口数量
4. **步骤3每完成一次检索**：更新"步骤3 检索计划"表格对应行状态为 `✅ 已完成`，填写结果文件路径
5. **步骤3全部完成**：更新执行进度表步骤3为 `✅ 完成`，步骤4改为 `⏳ 进行中`
6. **步骤4完成**：更新执行进度表步骤4为 `✅ 完成`，步骤5改为 `⏳ 进行中`
7. **步骤5完成（任务最终完成）**：
   - 更新执行进度表步骤5为 `✅ 完成`
   - 更新"最终输出"中的完成状态为 `✅ 已完成`
   - 填写实际文件大小和字数

**更新方式：** 直接使用 `write_file`（或等效工具）覆盖写入完整更新后的计划文件，保持文件始终反映最新状态。
**禁止：** 跳过更新、仅在对话中声明"已更新"而不实际写入文件。

---

### 【步骤 1】逐批解读全部上传文档

**目标：** 对每份参考文献的 `.md` 转换文件生成结构化解读文件，提取可引用的事实并分配证据编号。

**读取路径：** `/mnt/user-data/uploads/` 目录下**除 `{TASK_NAME}.md` 以外**的所有 `.md` 文件（这些是 PDF→Markdown 转换结果，是原始海报内容，不是总结产出物）

**批次处理规则（极其重要）：**
- 每批最多3个文件并行处理
- 每批完成后**立即自动启动下一批**，不得停顿，不得追问
- 必须处理完**全部文件**后才进入步骤2
- 禁止因"两种方案"而停下来询问：始终选择"继续下一批"
- 批次进度自动跟踪，当前批次完成 → 下一批次立即开始，无需用户确认

**⚠️ 每批强制执行序列（零例外，必须严格按此顺序）：**

```
对每批 3 个文件：

┌─ 第①步：派发子任务 ─────────────────────────────────────────┐
│  并行派发 3 个 task 子任务，等待全部返回结果                     │
└─────────────────────────────────────────────────────────────┘
         ↓（3个子任务全部返回后）
┌─ 第②步：逐一写文件（最关键步骤！）───────────────────────────┐
│  write_file("workspace/doc_summaries/[文件1]_解读.md", 结果1)  │
│  → ls workspace/doc_summaries/[文件1]_解读.md  ← 验证存在     │
│  write_file("workspace/doc_summaries/[文件2]_解读.md", 结果2)  │
│  → ls workspace/doc_summaries/[文件2]_解读.md  ← 验证存在     │
│  write_file("workspace/doc_summaries/[文件3]_解读.md", 结果3)  │
│  → ls workspace/doc_summaries/[文件3]_解读.md  ← 验证存在     │
└─────────────────────────────────────────────────────────────┘
         ↓（3个文件全部验证存在后）
┌─ 第③步：更新进度 ─────────────────────────────────────────────┐
│  write_file 更新计划文件（标记已完成文件、更新进度数字）          │
│  write_todos 更新 todo 状态                                    │
└─────────────────────────────────────────────────────────────┘
         ↓
┌─ 第④步：继续下一批 ───────────────────────────────────────────┐
│  立即开始下一批，不停顿，不追问                                  │
└─────────────────────────────────────────────────────────────┘
```

**🚨 违反序列的后果（已在4次运行中验证）：**
- 跳过第②步直接执行第③步 → 解读内容只存在于消息中 → Summarization 压缩后永久丢失 → 循环60+步后超时
- 在第②步之前加载 skill → 改变工作流 → 延迟/跳过 write_file → 同上
- 第②步中不做 ls 验证 → 写入可能静默失败 → 步骤2读取空目录

**每份解读文件结构（无内容填"文档未披露"，禁止填写编造数据）：**

```markdown
## 文件解读：[文件名]

### 基本信息
- 文件来源：[公司/机构名称]
- 对应资产/化合物：[名称 + 编号]
- Modality：[小分子 / ADC / 分子胶 / 双抗 / 其他]
- 主题关键词：[2-5个]
- 证据编号前缀：[D-xx 系列起始编号，如 D-01~D-08]

### 核心内容提炼（事实型，不编造）
- 靶点与机制：[文档中明确描述的内容，标注证据编号]
- 结合状态：[ON-state / OFF-state / Dual-state / tri-complex / 分子胶 / 文档未披露]
- 关键量化数据：[IC50/ORR/TGI/PK参数，有则列出并标注 D-xx；无则写"文档未披露 [G-xx]"]
- 创新点/差异化主张：[1-3条，直接引用文档原文或数据]
- 适应症与患者人群：[文档中明确的适应症]
- 研究阶段与模型：[临床前/I期/II期 + 使用的细胞系/动物模型]

### 竞争相关信息
- 与已上市药物的对比：[文档中明确的对比数据；无则写"文档未披露"]
- 联合用药线索：[文档中的联用实验或建议]
- 耐药机制线索：[文档中的耐药相关数据]

### 可引用内容
- 核心结论（2-5条，引用文档原文语句或数据，标注证据编号）
- 数据可靠性：[已验证（文档明确展示）/ ⚠️ 需核实（来源不清）]

### 证据编号清单
| 编号 | 类型 | 内容摘要 | 文档位置/图号 |
|------|------|---------|-------------|
| D-xx | 文档事实 | ... | 图X/正文 |
| G-xx | 信息缺口 | 未披露项 | — |
```

**保存路径：** `/mnt/user-data/workspace/doc_summaries/[原文件名]_解读.md`

**⚠️ 步骤1完成验证（全批次完成后强制执行）：**
```
# 列出所有解读文件
ls /mnt/user-data/workspace/doc_summaries/

# 计数
ls /mnt/user-data/workspace/doc_summaries/ | wc -l

# 预期：文件数 = 参考文献总数 M
# 若不等于 M：找出缺失文件，立即补写
```

**步骤1完成，必须立即输出此行：**
> ✅ 步骤1完成：共解读 [N] 份文件，已验证全部存在 → 立即执行步骤2

---

### 【步骤 2】生成综合素材总结报告

**前置条件验证（进入步骤2前强制执行）：**
```
ls /mnt/user-data/workspace/doc_summaries/ | wc -l
# 必须 ≥ 1，否则回退到步骤1
```

**保存路径：** `/mnt/user-data/workspace/master_summary.md`

**执行流程：**
1. 读取 `/mnt/user-data/workspace/doc_summaries/` 下所有解读文件
2. 综合所有解读，生成综合总结
3. **写入磁盘**：`write_file("/mnt/user-data/workspace/master_summary.md", 完整内容)`
4. **验证存在**：`ls -lh /mnt/user-data/workspace/master_summary.md`
5. 验证失败 → 重写（最多3次）

**必须包含以下七个章节（不得省略）：**

```markdown
# 综合素材总结报告

## 1. 资产全景清单
| 化合物/资产 | Modality | 靶点 | 结合状态 | 机制类型 | 开发阶段 | 来源文件 |

## 2. 核心量化数据汇总（仅文档已明确披露的数据，标注证据编号）
| 化合物 | 数据类型 | 数值 | 实验模型 | 来源文件 | 证据编号 | 可靠性 |

## 3. 机制与靶点图谱
（各资产靶向通路节点、作用位点差异、tri-complex/分子胶/ON-state/OFF-state 机制要点）

## 4. 科学故事线概览
（各资产的核心科学假设 → 关键实验链条 → 主要结论，不堆砌数据）

## 5. 竞争格局初步判断
（谁临床最先进、谁机制最差异化、谁有耐药挑战；基于已知事实，不做主观评分）

## 6. 联合用药与耐药线索汇总

## 7. 信息缺口清单（供步骤3检索；标注 G-xx 编号）
（列出上传文件未覆盖的信息：上市竞品疗效数据、流行病学、专利状态、已失败项目等）
```

**步骤2完成，必须立即输出此行：**
> ✅ 步骤2完成：master_summary.md 已生成并验证（[X] KB），[N] 个信息缺口 → 立即执行步骤3

**步骤2完成后必须更新计划文件：** 步骤2→✅完成（信息缺口[N]个），步骤3→⏳进行中

---

### 【步骤 3】在线检索补充信息

**前置条件验证：**
```
ls -lh /mnt/user-data/workspace/master_summary.md
# 必须存在，否则回退到步骤2
```

**重要：检索工具不可用时的处理规则**
- 若检索工具报错或无法联网 → **不停顿，不追问**，直接使用训练知识中的已知信息填充，全部标注⚠️未验证（C-xx 或 L-xx 编号），继续执行
- 禁止因检索失败而暂停或寻求用户确认
- 禁止输出"（模拟检索）"等提示代替实际内容 → 直接输出内容并标注来源和可靠性

**允许的检索来源（优先以下类别）：**
- ClinicalTrials.gov / EU Clinical Trials / ChiCTR / CDE
- PubMed / PMC / bioRxiv / medRxiv
- Google Patents / WIPO / USPTO / EPO / CNIPA
- NCBI Gene / UniProt / Open Targets / ChEMBL / BindingDB
- 公司官网、新闻稿、年报、SEC文件、投资者材料
- 已上市竞品说明书、FDA/NMPA审批信息、真实世界数据

**每次检索结果保存为：** `/mnt/user-data/workspace/search_results/[检索主题].md`

**⚠️ 每次检索的强制执行序列：**
```
对每个检索主题（共6个）：

1. 执行检索（或使用训练知识填充）
2. 生成检索结果 markdown 内容
3. write_file("/mnt/user-data/workspace/search_results/[主题名].md", 内容)
4. ls /mnt/user-data/workspace/search_results/[主题名].md  ← 验证存在
5. 验证失败 → 重写（最多3次）
6. 更新计划文件对应行
7. 继续下一个主题
```

**每个检索文件结构：**
```markdown
# 检索结果：[主题]

## 检索信息
- 来源网站：[URL]
- 检索关键词：[关键词]
- 检索日期：[日期]

## 采纳内容（已验证）
| 证据编号 | 内容 | 来源URL |
|---------|------|--------|
| DB-xx/CT-xx/C-xx | ... | ... |

## 未验证内容
| 证据编号 | 内容 | ⚠️ 状态 |
|---------|------|--------|
| G-xx | 检索未发现 | ⚠️ 未验证 |
```

**必须检索的6个主题由步骤0阶段一分析结果决定（任务专属，替换计划文件中的占位内容）。**
**默认参考方向（需根据实际任务调整）：**
1. 核心靶点/机制在研临床管线（ClinicalTrials.gov）
2. 已上市竞品疗效与安全性数据（FDA/公司官网）
3. 目标适应症全球+中国患者规模（PubMed/患病率数据库）
4. 核心化合物/平台专利布局（Google Patents/CNIPA）
5. 相关靶点/机制市场规模与预测（投资者材料）
6. 已失败/终止的相关项目（PubMed/ClinicalTrials.gov）

**⚠️ 步骤3完成验证（全部检索后强制执行）：**
```
ls /mnt/user-data/workspace/search_results/
ls /mnt/user-data/workspace/search_results/ | wc -l
# 预期：6 个文件。若不足，找出缺失主题立即补写
```

**步骤3完成，必须立即输出此行：**
> ✅ 步骤3完成：共完成 [N] 次检索（已验证 [N] 个文件存在），采纳 [M] 条信息 → 立即执行步骤4

**步骤3每次检索后必须更新计划文件：** 对应检索条目状态→✅已完成，填写结果文件路径
**步骤3全部完成后必须更新计划文件：** 步骤3→✅完成，步骤4→⏳进行中

---

### 【步骤 4】分章节撰写最终报告

**前置条件验证（进入步骤4前强制执行）：**
```
ls /mnt/user-data/workspace/doc_summaries/ | wc -l     # 必须 > 0
ls -lh /mnt/user-data/workspace/master_summary.md      # 必须存在
ls /mnt/user-data/workspace/search_results/ | wc -l    # 必须 > 0
```
任何一项为0或不存在 → 回退到对应步骤重新执行。

**执行前强制读取（不得跳过）：**
- `/mnt/user-data/workspace/doc_summaries/` 下所有解读文件
- `/mnt/user-data/workspace/master_summary.md`
- `/mnt/user-data/workspace/search_results/` 下所有检索结果

**写作原则（极其重要）：**
- **正文是叙事型分析**：用自然语言阐释科学故事线、对比、解释、综合，不堆砌原始数据表格
- **附录是事实库**：所有原始数据表、证据链、检索原始结果统一放附录
- **正文引用附录**：正文中关键事实用证据编号引用（如 [D-01]、[CT-03]），不在正文展开原始数据
- **正文最多使用一个高度浓缩的摘要表**，详细表格必须放附录
- **不编造不推断**：遵守原则E，未披露信息直接标注 G-xx
- **不输出主观判断**：禁止输出投资判断、立项建议、BD建议、Go/No-Go、商业预测、主观风险评分

**⚠️ 步骤4分章节写入策略（最重要的改动）：**

步骤4**不再一次性输出全文**，而是**逐章写入独立文件**，每章写完立即落盘验证：

```
创建目录: /mnt/user-data/workspace/report_chapters/

章节写入序列（严格顺序）：

1. 写第0章 → write_file("workspace/report_chapters/ch0_scope.md", 内容)
   → ls workspace/report_chapters/ch0_scope.md ← 验证
2. 写第一章 → write_file("workspace/report_chapters/ch1_summary.md", 内容)
   → ls workspace/report_chapters/ch1_summary.md ← 验证
3. 写第二章 → write_file("workspace/report_chapters/ch2_mechanism.md", 内容)
   → ls workspace/report_chapters/ch2_mechanism.md ← 验证
4. 写第三章 → write_file("workspace/report_chapters/ch3_competition.md", 内容)
   → ls workspace/report_chapters/ch3_competition.md ← 验证
5. 写第四章 → write_file("workspace/report_chapters/ch4_market.md", 内容)
   → ls workspace/report_chapters/ch4_market.md ← 验证
6. 写第五章 → write_file("workspace/report_chapters/ch5_innovation.md", 内容)
   → ls workspace/report_chapters/ch5_innovation.md ← 验证
7. 写第六章 → write_file("workspace/report_chapters/ch6_milestones.md", 内容)
   → ls workspace/report_chapters/ch6_milestones.md ← 验证
8. 写第七章 → write_file("workspace/report_chapters/ch7_gaps.md", 内容)
   → ls workspace/report_chapters/ch7_gaps.md ← 验证
9. 写附录 → write_file("workspace/report_chapters/appendices.md", 内容)
   → ls workspace/report_chapters/appendices.md ← 验证
```

**为什么分章写入：**
- 每章独立落盘 → 即使 Summarization 压缩消息，内容已安全存储
- 任何一章写入失败可单独重试，不丢失其他章节
- 步骤5汇总时从磁盘读取，不依赖消息上下文

**每章写入后的验证逻辑：**
```
write_file(path, content)
result = ls(path)
if NOT EXISTS:
    retry 1: write_file(path, content) → ls(path)
    if NOT EXISTS:
        retry 2: write_file(path, content) → ls(path)
        if NOT EXISTS:
            log_error, 继续下一章
```

---

**报告结构（正文七章 + 附录，全部必须输出）：**

### 报告标题
`{TASK_NAME} 深度总结分析报告 — 2026 AACR 年会`

---

#### 第 0 章：报告范围与文档类型识别（→ ch0_scope.md）
内容要求（bullet，200字内）：
- 输入材料清单（主题文件 + 参考文献数量）
- 自动识别的 Modality 类型（小分子 / 分子胶 / ADC / 双抗 / 其他）及判断依据
- 本报告仅做事实总结、科学解释和证据链分析，不做投资、立项、BD 或 Go/No-Go 判断
- 主要信息来源：步骤1解读文件 [D-xx] + 步骤3检索结果 [CT-xx/DB-xx/C-xx/P-xx]

---

#### 第一章：执行摘要（→ ch1_summary.md，300字内）
用高度浓缩的语言概括：
- 本批海报报道了哪些资产，覆盖哪些靶点/机制/适应症
- 技术演进主线（如：从 KRAS G12C 口袋式抑制 → RAS(ON) tri-complex / 分子胶 → pan-RAS 范式）
- 最关键的量化发现（引用证据编号，不展开数据）
- 主要信息缺口（仅列名）

> 要求：不做投资判断，不做立项建议，全程引用证据编号

---

#### 第二章：核心机制与科学故事线（→ ch2_mechanism.md，800-1200字，叙事型）

本章写科学叙事主线，不堆砌数据表，不重复附录内容。

需要说明：
- 这批海报共同回答了什么科学问题？
- 各资产从什么机制假设出发，用哪些实验链条验证？
- 作用原理与现有标准治疗的关键差异在哪里？
- tri-complex / 分子胶 / ON-state / dual-state 等范式的核心创新是什么？
- 哪些机制环节有直接文档证据 [D-xx]，哪些依赖外部数据库 [DB-xx]，哪些仍缺失 [G-xx]？

> 原始实验数据、图表说明、数据表全部放附录 B/C

---

#### 第三章：行业现状与竞争格局（→ ch3_competition.md，800-1400字，叙事型 + 一张浓缩竞品摘要表）

**3.1 已上市竞品**（叙事，引用证据编号；详细表格放附录 N）

**3.2 临床阶段管线**（叙事为主，引用证据编号；详细管线表放附录 M）
- 说明最新临床进展、各公司节奏差异、适应症布局特点
- 国内外管线的主要差异

**3.3 专利格局**（叙事，引用 P-xx；详细专利表放附录 O）
- 核心化合物/平台专利保护范围
- 中国专利申请/授权状况

**3.4 失败案例与教训**（叙事，引用证据编号；详细表格放附录 Q）
- 哪些项目失败/终止，原因
- 对在研项目的启示

> 正文最多一张简洁竞品摘要表（≤8行），详细表格放附录

---

#### 第四章：市场与流行病学（→ ch4_market.md，500-800字，叙事型）
- 目标适应症全球 + 中国患者规模（引用 DB-xx/L-xx，未检索到标注 G-xx）
- 现有标准治疗覆盖率与未被满足需求
- 相关市场规模参考数据（引用 C-xx/L-xx）

> 数据表格放附录 D

---

#### 第五章：关键创新点与差异化分析（→ ch5_innovation.md，600-1000字，叙事型）
- 这批资产相对现有疗法的核心创新（机制层面）
- 各资产之间的差异化定位（靶点选择性、Modality、适应症、联合策略）
- 可差异化的患者分层或生物标志物策略
- 已验证的生物标志物/选择性标准（引用 D-xx）
- 信息缺口对差异化判断的影响（引用 G-xx）

> 不做"谁更有潜力"的主观评分

---

#### 第六章：阶段性成果与里程碑数据（→ ch6_milestones.md，600-1000字，叙事型 + 数据摘要表）

用叙事方式总结各资产的关键数据节点，附一张数据摘要表（≤12行）：

| 化合物 | 数据类型 | 数值 | 实验模型/患者人群 | 证据编号 | 可靠性 |
|-------|---------|------|----------------|---------|------|

叙事需涵盖：
- 关键药效终点（IC50、TGI、ORR、PFS、OS、CR率等有文档证据的数据）
- 安全性数据摘要（DLT、3/4级 AE 发生率；无则标注 G-xx）
- PK/PD 参数（口服生物利用度、暴露量、t½；无则标注 G-xx）
- 生物标志物相关性数据

> 完整数据表放附录 B/H/I/J；正文不重复附录内容

---

#### 第七章：信息缺口与事实证据链总结（→ ch7_gaps.md，400-700字，叙事型）
- 文档未披露的信息集中在哪些方面？为什么影响对数据的理解？
- 哪些缺口可通过公开资料补充（专利/文献/数据库/公司公告）？
- 整体事实证据链：从机制假设→体外→体内→PK→安全性→临床，哪些环节有证据，哪些缺失？

> 完整缺口表和证据链表放附录 R/S；不做"建议"式决策性表述

---

### 附录（→ appendices.md，事实库，按需输出，不适用的附录跳过）

#### 附录 A：文档一页事实卡（每篇参考文献一行摘要）
| 文件名 | 资产 | Modality | 靶点 | 最关键数据 | 证据编号 |

#### 附录 B：原始数据抽取表（来自步骤1解读文件）
| 化合物 | 图号/实验 | 实验系统 | 处理方式 | Readout | 关键结果 | 统计学 | 是否完整披露 | 证据编号 |

#### 附录 C：科学故事线证据表
| 步骤 | 文档展示数据 | 实验系统 | 结果 | 该数据能说明 | 该数据不能说明 | 证据编号 |

#### 附录 D：疾病与流行病学数据
| 适应症 | 地区 | 患者规模 | 年份 | 来源 | 链接 | 证据编号 |

#### 附录 E：靶点与通路数据库信息
| 靶点 | 数据库 | 关键信息 | 与文档关系 | 链接 | 证据编号 |

#### 附录 F：Modality 特色字段表
根据识别的 Modality 类型输出对应字段：

**小分子**：compound ID、scaffold、biochemical potency、cell potency、selectivity panel、binding pocket、oral bioavailability、half-life、CYP/hERG、PK参数

**分子胶 / PROTAC**：target protein、E3 ligase、DC50、Dmax、degradation kinetics、ternary complex、hook effect、proteomics selectivity、cell permeability

**ADC**：antigen、internalization、linker type、payload、DAR、bystander effect、plasma stability、ADC comparator

**双抗 / TCE**：tumor antigen、immune arm、affinity tuning、CRS数据、T cell killing assay、cynomolgus cross-reactivity

| Modality | 字段 | 文档事实 | 外部公开信息 | 是否披露 | 证据编号 |

#### 附录 G：药物/化合物公开信息
| 名称 | 别名 | Modality | 靶点 | 效价 | 选择性 | PK | 给药方式 | 与文档是否一致 | 证据编号 |

#### 附录 H：体外实验数据
| 细胞系 | 基因背景 | 化合物 | 浓度 | Readout | 结果 | 对照 | 证据编号 |

#### 附录 I：动物实验数据
| 模型 | 动物 | 样本量 | 给药方案 | 主要结果 | PD marker | 毒性观察 | 证据编号 |

#### 附录 J：安全性与耐受性数据
| 数据类型 | 模型/物种 | 剂量 | 观察指标 | 结果 | 是否披露 | 证据编号 |

#### 附录 K：生物标志物相关事实
| Biomarker | 类型 | 检测方式 | 文档是否披露 | 可能用途 | 证据编号 |

#### 附录 L：文献检索结果
| PMID/DOI | 年份 | 标题 | 期刊 | 研究类型 | 与文档关系 | 链接 | 证据编号 |

#### 附录 M：临床登记检索结果（穷举，宁多勿少）
| 登记号 | 药物 | 靶点/机制 | 公司 | 适应症 | 阶段 | 状态 | 主要终点 | 链接 | 证据编号 |

#### 附录 N：同靶点/同机制竞品对比
| 类别 | 项目 | 公司 | Modality | 阶段 | 适应症 | 公开数据 | 与主题异同 | 证据编号 |

#### 附录 O：专利检索结果
| 专利号 | 申请人 | 标题 | 优先权日 | 公开日 | 涉及主题 | 与文档关系 | 链接 | 证据编号 |

#### 附录 P：文档与外部信息一致性核查
| 核查项 | 文档信息 | 外部公开信息 | 是否一致 | 差异描述 | 证据编号 |

#### 附录 Q：已失败/终止项目
| 项目名称 | 靶点 | 开发阶段 | 终止时间 | 公开原因 | 来源 | 证据编号 |

#### 附录 R：信息缺口清单
| 类别 | 未披露信息 | 影响理解的原因 | 可通过何种资料补充 | 证据编号 |

#### 附录 S：事实证据链表
| 环节 | 文档事实 | 外部事实 | 是否有公开证据 | 缺失信息 | 证据编号 |

#### 附录 T：检索式与来源清单
| 检索类别 | 关键词 | 数据源 | 检索日期 | 命中摘要 | 链接 |

---

> **引用规则**：正文所有关键数据以证据编号（如 [D-01]、[CT-03]）内联标注，对应附录中的完整信息。无法核实的内容统一标注 ⚠️ 未验证。禁止在正文中展开已放入附录的原始数据表。

**⚠️ 步骤4完成验证（所有章节写入后强制执行）：**
```
ls /mnt/user-data/workspace/report_chapters/
ls /mnt/user-data/workspace/report_chapters/ | wc -l
# 预期：≥ 9 个文件（ch0~ch7 + appendices）
# 若缺失任何文件，立即补写
```

**步骤4完成，必须立即输出此行：**
> ✅ 步骤4完成：报告 [N] 个章节文件已全部写入并验证 → 立即执行步骤5

**步骤4完成后必须更新计划文件：** 步骤4→✅完成，步骤5→⏳进行中

---

### 【步骤 5】汇总章节、保存文件并呈现

**步骤5的核心任务：从磁盘读取各章节文件，拼接为完整报告，写入最终输出路径。**

**⚠️ 强制执行序列（5个动作，全部完成才算步骤5完成）：**

**动作1：读取所有章节文件并拼接**
```
读取顺序（严格）：
1. workspace/report_chapters/ch0_scope.md
2. workspace/report_chapters/ch1_summary.md
3. workspace/report_chapters/ch2_mechanism.md
4. workspace/report_chapters/ch3_competition.md
5. workspace/report_chapters/ch4_market.md
6. workspace/report_chapters/ch5_innovation.md
7. workspace/report_chapters/ch6_milestones.md
8. workspace/report_chapters/ch7_gaps.md
9. workspace/report_chapters/appendices.md

拼接规则：
- 报告标题行："{TASK_NAME} 深度总结分析报告 — 2026 AACR 年会"
- 各章节之间添加 "---" 分隔
- 末尾添加生成时间戳
```

**动作2：写入最终报告文件**
```
write_file("/mnt/user-data/outputs/{TASK_NAME}_report.md", 拼接后的完整内容)
```

**动作3：验证文件存在与大小**
```
ls -lh "/mnt/user-data/outputs/{TASK_NAME}_report.md"
```
- 若文件不存在 → 立即重写（最多3次）
- 若文件大小 < 10KB → 可能内容缺失，检查并重写

**动作4：调用 `present_files` 工具展示该文件路径**

**动作5：输出完成摘要**
```
✅ 任务完成

中间产出物：
- 文档解读：/mnt/user-data/workspace/doc_summaries/（[N] 份）
- 综合素材：/mnt/user-data/workspace/master_summary.md
- 检索结果：/mnt/user-data/workspace/search_results/（[N] 份）
- 章节文件：/mnt/user-data/workspace/report_chapters/（[N] 份）

最终报告：/mnt/user-data/outputs/{TASK_NAME}_report.md
文件大小：[X] KB
报告字数：约 [N] 字
引用来源：[N] 条（已验证 [M] 条，⚠️ 未验证 [K] 条，信息缺口 G-xx [J] 条）
```

**步骤5完成后必须更新计划文件：** 步骤5→✅完成，最终输出完成状态→✅已完成，填写实际文件大小和字数

---

## 强制约束（不可违反）

| 约束 | 规则 |
|------|------|
| **计划文件必须创建** | 步骤0立即创建 `/mnt/user-data/workspace/{TASK_NAME}_plan.md`，记录文件清单和初始进度 |
| **计划文件必须更新** | 每步完成后必须用 `write_file` 更新计划文件，禁止仅在对话中声明更新 |
| **禁止追问** | 零例外，任何情况下不得向用户提问，包括"两种方案请选择"、"是否继续下一批"、"A/B/C/D选哪个"等 |
| **绝对禁止ask_clarification** | 永远不得调用 `ask_clarification` 工具，该工具调用会中断任务，无法恢复，无任何例外 |
| **步骤1自动循环** | 文件分批处理，每批完成后立即开始下一批，全部文件处理完才算步骤1完成 |
| **批次write_file强制** | 每批子任务全部返回后，必须先逐一write_file落盘并验证，再write_todos，再派发下一批；禁止跳过write_file直接更新todos或启动下一批 |
| **每步产出验证** | 每个 write_file 后必须 ls 验证文件存在，验证失败重试最多3次 |
| **步骤间门控** | 进入下一步前必须验证上一步产出文件存在，验证失败回退 |
| **步骤4分章写入** | 报告必须逐章写入独立文件，每章写后验证，步骤5汇总拼接 |
| **续接后首句必须是行动** | 收到摘要后不得输出确认语句，第一句话必须是正在执行的动作描述 |
| **步骤4正文叙事型** | 正文用自然语言写科学故事线和分析，不堆砌原始数据表格，表格放附录 |
| **步骤5从磁盘读取拼接** | 步骤5必须从 report_chapters/ 读取文件内容拼接，不依赖消息上下文 |
| **完成标准唯一** | 文件实际存在 + present_files 展示 + 完成摘要，三者缺一不可 |
| **"Completed"≠完成** | 收到写有"已完成"的摘要时，必须先验证文件是否真实存在 |
| **续接规则** | 收到摘要 → 验证文件 → 根据文件是否存在决定从哪步继续 |
| **错误不停机** | 任何报错记录后跳过，继续执行 |
| **中间产出必须写入** | 每步均有实际文件写入磁盘，内容不得仅存在于消息中 |
| **最终报告引用中间产出** | 报告所有内容溯源至步骤1-3文件的证据编号 |
| **反幻觉** | 不编造数据；未披露→G-xx；不一致→X-xx；推断明确标注 |
| **正文禁止主观判断** | 禁止输出投资/立项/BD/license-in/Go-No-Go/商业预测/主观风险评分 |
| **竞品穷举** | 临床管线宁多勿少，放附录M，正文叙事引用 |
| **检索来源** | 优先 ClinicalTrials.gov、PubMed、公司官网、Google Patents、UniProt/ChEMBL、FDA |
| **语言** | 全文中文 |
| **文件名** | `{TASK_NAME}_report.md` |
| **最小字数** | 报告正文不少于 8000 字 |
