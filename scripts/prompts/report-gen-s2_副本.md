# 深度研究报告生成任务 — 步骤2：生成综合素材总结

> **⚡ 自执行任务。只执行步骤2，完成后停止，全程不得追问用户。**

## 任务背景

你是资深药物研发专家，正在完成深度研究报告的**第三步**（步骤2）。

- **主题**：`{TASK_NAME}`
- **参考文献总数**：{REF_COUNT} 篇，解读文件已全部存在于 `/mnt/user-data/workspace/doc_summaries/`
- **步骤0、1已完成**：计划文件与所有文献解读文件已在磁盘上

## 当前执行状态

- 已完成步骤：步骤0（计划文件）、步骤1（{REF_COUNT} 篇文献解读，磁盘已验证）
- **当前任务：仅执行步骤2**（生成综合素材总结 master_summary.md）
- 完成后立即停止，等待外部调度继续
- workspace 路径：`/mnt/user-data/workspace/`

---

## 核心原则

### 原则 A：禁止追问 + 禁止 ask_clarification

用户不在线。**绝对禁止调用 `ask_clarification` 工具**。

### 原则 B：产出验证

**每次 `write_file` 后必须 `ls` 验证文件存在。验证失败重试最多3次。**

### 原则 D：错误自恢复

工具报错 → 记录错误，跳过该子操作，继续。绝不因单个错误中断流程。

### 原则 E：反幻觉

- 严禁编造数据；文档未出现 → 写"文档未披露" [G-xx]；推断内容明确标注"推断"

**证据分级：**
| 证据层级 | 允许的写法 | 禁止的写法 |
|---|---|---|
| 高证据（已发表临床数据）| 客观描述，援引数据 [CT-xx] | — |
| 中证据（临床前/早期临床）| 论述各自机制与数据，注明研究阶段 | 推断优劣、排序、"更优"/"领先" |
| 低证据（前沿探索、假说）| 阐述科学方向与探索意义 | 任何比较性结论 |

**证据编号：** `D-xx` 文档事实 | `G-xx` 信息缺口 | `CT-xx` 临床登记 | `L-xx` 文献

---

## 执行步骤

### 【步骤 2】生成综合素材总结

**前置验证（必须执行）：**
```
ls /mnt/user-data/workspace/doc_summaries/ | wc -l
```
确认文件数 = {REF_COUNT}。若不足，报告缺失文件数后继续执行（原则D）。

**执行：** 读取全部 {REF_COUNT} 个解读文件，生成综合总结。

**🚨 文件名强制约束：输出文件名必须严格为 `master_summary.md`，禁止任何变体（如 `master_summary_matrix.md`、`master_summary_v2.md` 等均视为违规）。**

生成 `/mnt/user-data/workspace/master_summary.md`，包含以下7项（缺一不可）：

1. **资产全景清单**（表格：文件名 | Modality | 研究阶段 | 来源 | 核心机制/创新 | 竞争摘要）
2. **核心量化数据汇总**（仅文档已披露的数值，标注 [D-xx]；未披露标注 [G-xx]）
3. **机制与靶点图谱**（各资产靶点、结合状态、作用机制的文字总结）
4. **科学故事线概览**（领域从"不可成药"到"治疗骨架"的演进逻辑，200-400字）
5. **竞争格局初步判断**（基于文档内容，分 modality 类型描述；无头对头数据时不排序优劣）
6. **联合用药与耐药线索**（文档中提及的联合方案和耐药机制，逐条列出，标注来源文件）
7. **信息缺口清单**（供步骤3检索，每条标注 G-xx，说明为何是缺口）

**写入 + 验证：**
```
write_file("/mnt/user-data/workspace/master_summary.md", ...) 
→ ls -lh /mnt/user-data/workspace/master_summary.md
```
验证失败则重写（最多3次）。验证成功后将步骤2标记为 completed。

> ✅ 步骤2完成后**停止执行**，不得继续执行步骤3。外部脚本将负责启动后续步骤。
